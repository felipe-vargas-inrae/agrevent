{
  "name": "agrevent_storage",
  "version": "1.0.0",
  "description": "A project for management the entry of data to the database based on a kafka broker listener",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@felipex",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "kafkajs": "^1.10.0",
    "mongoose": "^5.2.15",
    "dotenv": "^6.1.0",
    "rootpath": "^0.1.2"
  }
}
