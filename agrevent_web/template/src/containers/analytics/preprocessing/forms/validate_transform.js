const validate = values => {
  const errors = {};
  // if (!values.name) {
  //   errors.name = 'Name field shouldn’t be empty';
  // }
  // if (!values.dataframeName) {
  //   errors.dataframeName = 'Please select the option';
  // }
  
  return errors;
};

export default validate;